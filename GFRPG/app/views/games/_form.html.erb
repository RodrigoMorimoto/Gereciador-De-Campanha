<%= form_with(model: game, local: true) do |form| %>
  <% if game.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(game.errors.count, "error") %> prohibited this game from being saved:</h2>

      <ul>
      <% game.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :Game_name %>
    <%= form.text_field :Game_name, id: :game_Game_name %>
  </div>

  <div class="field">
    <%= form.label :Game_description %>
    <%= form.text_area :Game_description, id: :game_Game_description %>
  </div>

  <div class="field">
    <%= form.label :Game_date %>
    <%= form.date_select :Game_date, id: :game_Game_date %>
  </div>

  <%= form.hidden_field :mestre_id, :value =>  cookies[:mestre] %>

  <div class="actions">
    <%= form.submit %>
  </div>

  

<% end %>



