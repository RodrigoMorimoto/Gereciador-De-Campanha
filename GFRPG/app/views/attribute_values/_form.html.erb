<%= form_with(model: attribute_value, local: true) do |form| %>
  <% if attribute_value.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(attribute_value.errors.count, "error") %> prohibited this attribute_value from being saved:</h2>

      <ul>
      <% attribute_value.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
